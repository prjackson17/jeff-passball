[project]
name = "mlb-rag"
version = "0.1.0"
description = "MLB Broadcast Intelligence: RAG-powered daily briefing system"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:BuildBackend"

[tool.setuptools.packages.find]
where = ["src"]